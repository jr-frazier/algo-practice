test("sorts a given array from smallest number to largest", () => {
  //   const arr = [9, 3, 2, 4, 7];
  const arr = [2, 3, 4, 7, 9];
  const arr2 = [2, 3, 4, 7, 9];

  expect(arr).toEqual(arr2);
  //   expect(selectionSort(arr)).toStrictEqual([2, 3, 4, 7, 9]);
});
